# Development mode - using in-memory storage
# KV variables are not set, so the API will fall back to memory storage

# Optional: Enable analytics in development
# NEXT_PUBLIC_ENABLE_ANALYTICS=true

# For production, you'll need to add KV credentials from Vercel dashboard:
# Get these from your Vercel KV dashboard (https://vercel.com/docs/storage/vercel-kv)
# KV_URL="rediss://default:YOUR_TOKEN@your-database.upstash.io:6379"
# KV_REST_API_URL="https://your-database.upstash.io"
# KV_REST_API_TOKEN="your_rest_api_token"
# KV_REST_API_READ_ONLY_TOKEN="your_read_only_token"
# REDIS_URL="rediss://default:YOUR_TOKEN@your-database.upstash.io:6379"